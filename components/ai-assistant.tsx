"use client";import { useMemo, useState } from "react";import { IntelligenceSnapshot } from "@/lib/types";import { Card, Button, Pill } from "@/components/ui";
export function AiAssistant({s}:{s:IntelligenceSnapshot}){const [q,setQ]=useState("Quais são os principais próximos passos?");const answer=useMemo(()=>{const risk=s.summary.risks[0];const opp=s.summary.opportunities[0];return `Recomendação Lumos: priorizar uma narrativa positiva baseada em nostalgia e novidades oficiais. Maior oportunidade: ${opp}. Principal atenção: ${risk}. Sugestão prática: montar um calendário semanal com social listening, PR angles e creators geek para responder rápido a picos de conversa.`},[s]);return <Card id="ai"><Pill>AI Assistant</Pill><textarea value={q} onChange={e=>setQ(e.target.value)} className="mt-4 min-h-24 w-full rounded-2xl border border-white/10 bg-black/30 p-4 text-parchment"/><Button className="mt-3">Ask Lumos AI</Button><div className="mt-4 rounded-2xl bg-gold/10 p-4 text-sm text-parchment/80">{answer}</div></Card>}
