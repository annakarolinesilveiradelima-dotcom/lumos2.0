import { IntelligenceSnapshot, SourceKind } from "@/lib/types";import { Card, Pill } from "@/components/ui";
export function ItemsPanel({s,kind,title}:{s:IntelligenceSnapshot;kind?:SourceKind;title:string}){const items=kind?s.items.filter(i=>i.sourceKind===kind):s.items;return <Card id={kind||'news'}><div className="mb-4 flex items-center justify-between"><Pill>{title}</Pill><span className="text-sm text-parchment/50">{items.length} itens</span></div><div className="thin-scroll max-h-[520px] space-y-3 overflow-auto pr-1">{items.map(i=><a key={i.id} href={i.url} target="_blank" className="block rounded-2xl border border-white/10 bg-black/20 p-4 transition hover:border-gold/40"><div className="flex flex-wrap items-center gap-2"><span className={`rounded-full px-2 py-1 text-xs ${i.sentiment==='positive'?'bg-gold/15 text-gold':'bg-red-500/15 text-red-300'}`}>{i.sentiment}</span><span className="text-xs text-parchment/45">{i.source} • {i.publishedAt.slice(0,10)}</span></div><h4 className="mt-2 font-semibold">{i.title}</h4><p className="mt-1 text-sm text-parchment/60">{i.summary}</p><p className="mt-2 text-xs text-gold/80">Por que: {i.sentimentReason}</p></a>)}</div></Card>}
